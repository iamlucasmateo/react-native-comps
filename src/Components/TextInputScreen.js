import React from 'react';
import { Text, InputText, StyleSheet, View } from 'react-native';

export const TextInputScreen = () => {
    return (
        <View>
            <Text>Text Input Screen</Text>
        </View>
    )
}

const styles = StyleSheet.create({})