import React, { useReducer } from 'react';
import { Text, View, TouchableOpacity, StyleSheet, FlatList } from 'react-native';

export const ColorChanger = () => {
    return (
        <View>
            <Text>Color Changer goes here</Text>
        </View>
    )
};

const styles = StyleSheet.create({});